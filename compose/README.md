This directory contains modular building block docker-compose files of atlas-base.
You can use them to quickly spin up a new atlas-base project. See example/compose/app.yml for an example how to build a project with atlas-base.
Have a look at /example/run.sh to see how to run the example project from an auto-composed docker-compose.yml file. 
