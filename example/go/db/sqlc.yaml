version: "2"
sql:
  - engine: postgresql
    queries: ./queries/
    schema: ./migrations/
    gen:
      go:
        package: db
        out: .
        emit_prepared_queries: false
        emit_interface: false
        emit_exact_table_names: false
        emit_json_tags: true
