# Code generated by schemaf codegen. DO NOT EDIT.
# Test environment: ephemeral postgres (no volumes) + project services.
# Started/stopped by ./schemaf.sh test. Postgres data is discarded on stop.

name: schemaf-example-test

include:
  - compose/clock.yml

services:
  postgres:
    image: postgres:17-alpine
    container_name: schemaf-example-postgres-test
    environment:
      POSTGRES_USER: schemaf
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: schemaf-example
    ports:
      - "127.0.0.1::5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U schemaf -d schemaf-example"]
      interval: 2s
      timeout: 2s
      retries: 10
      start_period: 5s

  clock:
    ports:
      - "127.0.0.1::8080"

networks:
  net:
    driver: bridge
    name: schemaf-example-test-net
